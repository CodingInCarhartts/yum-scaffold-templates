import { defineConfig } from 'vite'
{{#if use_tailwind}}import tailwindcss from '@tailwindcss/vite'{{/if}}

export default defineConfig({
  {{#if use_tailwind}}plugins: [tailwindcss()],{{/if}}
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist',
    sourcemap: true
  }
})